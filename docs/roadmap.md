# Roadmap

## Planned
- Streaming text generation.
- ONNX export for CPU.
- Optional FastAPI API mode.
- More Sinhala UI labels and examples.

## Known limitations
- Image generation quality depends on VQ training data.
- CPU inference is slower than GPU; 4GB RAM minimum.
- No built-in content filter; optional filter when user enables.
