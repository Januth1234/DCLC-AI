# Copy to config.local.yaml and set your paths.
# config.local.yaml is gitignored.

data:
  sinhala_path: "data/sinhala"
  image_caption_path: "data/image_captions.json"
  explicit_path: ""       # Optional extra text (file or dir of .txt). Raw is compulsory via preloaded default.
  raw_image_captions_path: ""  # Optional: JSON [{"caption":"..."}] or list of strings
  # Kaggle: use /kaggle/input/your-dataset/... for cloud data
